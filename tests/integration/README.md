# Integration Tests

Cross-service API, webhook, import/export, and database persistence tests belong here. Test successful writes, invalid input, duplicates, migrations, and container recreation using isolated test data.

Use each vendor's public interfaces for integrations. Direct SQL checks are for verifying the owning application's database behavior, not creating runtime coupling between products.
