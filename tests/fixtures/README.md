# Synthetic Fixtures

Keep small, deterministic sample inputs for imports, forms, and integration tests here. Never use real company records, credentials, or production database dumps.

Service-specific schema migrations and initialization scripts belong to the owning module. Fixtures supply test content, not a competing schema definition.
