# Copy to environments/development/mariadb/.env. Never commit the populated file.
# Metadata only; provision database credentials with the first runtime implementation.
PRISM_ENV=development
COMPOSE_PROJECT_NAME=prism-dev-mariadb
PRISM_DB_NETWORK=prism-dev-mariadb-data
TZ=Asia/Seoul
