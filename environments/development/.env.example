# Copy to environments/development/.env. Never commit the populated file.
# Metadata only until Compose definitions and lifecycle scripts are implemented.
PRISM_ENV=development
COMPOSE_PROJECT_NAME=prism-dev
TZ=Asia/Seoul
