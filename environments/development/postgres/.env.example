# Copy to environments/development/postgres/.env only when PostgreSQL is needed.
# Metadata only until an actual consuming application is implemented.
PRISM_ENV=development
COMPOSE_PROJECT_NAME=prism-dev-postgres
PRISM_DB_NETWORK=prism-dev-postgres-data
TZ=Asia/Seoul
