# Copy to environments/production/.env on the Ubuntu server only.
# Metadata only until Compose definitions and lifecycle scripts are implemented.
PRISM_ENV=production
COMPOSE_PROJECT_NAME=prism-prod
TZ=Asia/Seoul
