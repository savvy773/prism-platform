# Copy to environments/production/postgres/.env on the server only when needed.
# Metadata only; provision credentials independently of development and MariaDB.
PRISM_ENV=production
COMPOSE_PROJECT_NAME=prism-prod-postgres
PRISM_DB_NETWORK=prism-prod-postgres-data
TZ=Asia/Seoul
