# Copy to environments/production/mariadb/.env on the Ubuntu server only.
# Metadata only; provision database credentials independently of development.
PRISM_ENV=production
COMPOSE_PROJECT_NAME=prism-prod-mariadb
PRISM_DB_NETWORK=prism-prod-mariadb-data
TZ=Asia/Seoul
