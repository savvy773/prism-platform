# Docker Infrastructure

Shared Docker and Compose conventions for the Prism Platform.

This area will contain reusable network, volume, and Compose fragments only when they reduce duplication without making the setup harder to understand.

Early-stage rule: prefer explicit, readable Compose files over premature abstraction.
