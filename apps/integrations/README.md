# Integrations

Integration code and adapters between Prism applications and external/internal systems.

Examples:
- ERPNext API clients
- import/export adapters
- synchronization jobs
- webhook handlers

Keep integration boundaries explicit. Prefer APIs and documented data contracts over direct cross-application database coupling.
